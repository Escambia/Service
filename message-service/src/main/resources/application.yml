spring:
  webflux:
    base-path: "/escambia/message"
  application:
    name: "message-service"
  data:
    mongodb:
      uri: "mongodb://mongo1:27017,mongo2:27027,mongo3:27037/chat"
server:
  compression:
    enabled: true
  http2:
    enabled: true
  shutdown: graceful
  port: 8081
springdoc:
  webjars:
    prefix: ""
